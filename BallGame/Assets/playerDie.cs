using UnityEngine;
using System.Collections;

public class playerDie : MonoBehaviour {
	private int lives;

	void OnTriggerEnter (Collider playerSphereCollider){
		// player = (Player)GameObject.Find ("").GetComponent (typeof());

		// if(
	}
}
